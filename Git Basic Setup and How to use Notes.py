#1) git config --global user.name Shubham ( Uses for set the user name )

#2) git config --global user.name ( Check that user name is entered or not )

#3) git init :- Command is uses for initialization of file in git

#4) git status:- Command uses for check the status of file

#5) git add:- command uses for adding a file into git

#6) touch NAMEOFFILE.html :- uses for creating the files

#7) git add -A :- Command uses for adding all files in page at a time

#8) clear :- command is uses for clearn the terminal

#9) git checkout NANEOFFILE.html:- command is uses for again maintain the previos code of that page if erase or edited by mistake 

#10)git checkout -f :- IF all the files are edited or deleted by mistake then 

#11)git log :- is a command uses for showing that what what you have commited already

#12) git log -p -NOOFCOMMIT:- is a command uses for getting last no. of commit which we commited

#13) git diff:- is a command indicates that the difference you created in code by comparing given file to another file

#14) git ls:- is a command uses for adding and showing all the files in git page at a time

#15) git rm FILENAME :- command is uses for remove the file permanantly

#16) git rm- - cached FILENAME :- command is uses for clear the file from the working stage but not permnantly from the memory

#17) git status -s
     Green(M):- stage area me aapki file modify ho gyi h ..
     Red(M) :- Working Tree me aapki file modify ho gyi h

#18) Master Branch is that branch which is main branch and contain the actual code

#19)git checkout BRANCHNAME:- command uses for shifting MASTER BRANCH to another BRANCHNAME and if the BRANCHNAME is MASTER then it will move to master branch from that another branch
#20) git branch:- command uses for check that how many master branches we have?
     Git help section:-

    clone     Clone a repository into a new directory
   init      Create an empty Git repository or reinitialize an existing one

work on the current change (see also: git help everyday)
   add       Add file contents to the index
   mv        Move or rename a file, a directory, or a symlink
   restore   Restore working tree files
   rm        Remove files from the working tree and from the index

examine the history and state (see also: git help revisions)
   bisect    Use binary search to find the commit that introduced a bug
   diff      Show changes between commits, commit and working tree, etc
   grep      Print lines matching a pattern
   log       Show commit logs
   show      Show various types of objects
   status    Show the working tree status

grow, mark and tweak your common history
   branch    List, create, or delete branches
   commit    Record changes to the repository
   merge     Join two or more development histories together
   rebase    Reapply commits on top of another base tip
   reset     Reset current HEAD to the specified state
   switch    Switch branches
   tag       Create, list, delete or verify a tag object signed with GPG

collaborate (see also: git help workflows)
   fetch     Download objects and refs from another repository
   pull      Fetch from and integrate with another repository or a local branch
   push      Update remote refs along with associated objects

'git help -a' and 'git help -g' list available subcommands and some
concept guides. See 'git help <command>' or 'git help <concept>'
to read about a specific subcommand or concept.
See 'git help git' for an overview of the system.

#

     
     



